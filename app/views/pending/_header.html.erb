<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  
  <div class="container">
    
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/register/calendar"><b>nilpeter</b></a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= content_tag(:li, link_to("Calendar", :controller => "register", :action => 'calendar'), :class => ("active" if current_page? "/register/calendar" or current_page? "/") ) %>
        <%= content_tag(:li, link_to("Schedule", :controller => "register", :action => 'schedule'), :class => ("active" if current_page? "/register/schedule") ) %>
        <%= content_tag(:li, link_to("Table", :controller => "register", :action => 'index'), :class => ("active" if current_page? controller: 'register'or 
              current_page?(controller: 'register', action: 'edit') or current_page?(controller: 'register', action: 'delete') ) ) %>
        <%#= content_tag(:li, link_to("Hotlists", :controller => "hotlists", :action => 'index'), :class => ("active" if current_page? "/hotlists") ) %>
        
      </ul>
      <p class="navbar-text">Sales Visit Schedule</p>
      <ul class="nav navbar-nav navbar-right">
        <%#= content_tag(:li, link_to("Service", :controller => "service", :action => 'index'), :class => ("active" if current_page? "/service/index") ) %>
         <%= content_tag(:li, link_to("Admin", :controller => 'admin', :action => 'index'), :class => ("active" if current_page? "/pending") )  %>
        <% if current_user %>
           <%= content_tag(:li, link_to("Log Out", logout_path, :class => ("active" if current_page? "/admin"))) %>
         <%# else %>
           <%#= link_to "Sign Up", signup_path %>
           <%#= link_to "Log In", login_path %>
         <% end %>
       </ul>
    </div>
    
    
  </div>
</div><!--/.nav-collapse -->
<script src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>